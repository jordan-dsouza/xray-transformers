[project]
name = "xray-transformers"
version = "0.1.0"
description = "Unified interpretability and uncertainty toolkit for transformer models"
requires-python = ">=3.10"

[tool.black]
line-length = 88

[tool.pytest.ini_options]
testpaths = ["tests"]
